
type Props = {
  linkData: any[];
  ellipse: string;
  group: string;
  style: React.CSSProperties;
};

const page = ({ linkData, ellipse, group, style }: Props) => {
  return (
   <main>
 
   </main>
  )
}

export default page




